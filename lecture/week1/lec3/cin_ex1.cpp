#include<iostream>

using namespace std;

int main(void) {
    // cin - example 
    
    // First create variables and then take input from the user to put 
    // information into it.

    
    int x = 0;

    // First print out a line asking user to enter something
    cout << "Please enter a number" << endl;
    
    // This will wait for the user to enter a number and then press enter
    cin >> x;
    
    // Print out the number
    cout << "User input is : " << x << endl;
    

	return 0;
}