#include<iostream>
using namespace std;
int main(void) {
    // I've removed blank lines
    // Blank lines are ignored by the compiler. They make no difference
    // Program still produces the same output.
    // Choice of having empty lines between blocks is aesthetic.
    cout << "Hello 1300" << endl;
    return 0;
}