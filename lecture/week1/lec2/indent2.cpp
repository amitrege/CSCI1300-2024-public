#include<iostream>

using namespace std;

int main(void) {
    // Lines of code are demarcated by semi-colons
    // Theoretically, can write everything on one line (DO NOT DO THIS!)
    // Code still produces the same output as hello.cpp
    cout<<"Hello 1300"<< endl;return 0;
}