#include<iostream>

using namespace std;

int main(void) {
    
    // Removed the "return 0;" line
    // The code still compiles and produces the same output
    // Usually a good idea to still have it in however
    
    cout << "Hello 1300" << endl;
}