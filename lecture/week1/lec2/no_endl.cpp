#include <iostream>

using namespace std;

int main(void) {
    
    // Removed the 'endl' operator from the first line
    // The ouput will only have a single line - because 'endl' moves the 
    // output cursor to the next line
    
    // You can play around with removing and adding 'endl' from the 
    // second line too
    
    cout << "Hello 1300 ";
    
    cout << "Bye 1300" << endl; 

    return 0;
}